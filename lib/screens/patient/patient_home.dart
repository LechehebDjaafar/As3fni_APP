import 'package:flutter/material.dart';
import 'patient_main.dart';

class PatientHome extends StatelessWidget {
  const PatientHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const PatientMain();
  }
}
